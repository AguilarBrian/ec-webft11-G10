import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    container: {
        width: "1492px",
        left: "-146.111px",
    },
    banner: {
        top: "auto",
        bottom: "0",
        minHeight: "25%",
        height: "auto",
        maxHeight: "100%",
        textAlign: "left",
    }
});